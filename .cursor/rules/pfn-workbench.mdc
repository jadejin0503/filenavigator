---
description: 项目管理-工作台页：卡片网格与“永远打开最新版 exe”规则
globs: app_qt.py
alwaysApply: false
---

# 工作台（Project Management -> Workbench）

当修改 `app_qt.py` 的项目管理右侧 Tab（`self.pm_tabs`）时：

- **标签页位置**：`我的待办`、`项目数据分析`、`工作台` 并排；`工作台`固定为第三个。
- **布局风格**：复用项目管理区域的卡片视觉（白底、细边框、圆角、轻阴影），使用**统一尺寸**的网格卡片，间距一致。
- **工具顺序固定**：`PDTManager` → `QCT_Tools` → `RTFtoPDF`。
- **启动规则**：
  - 点击卡片时**每次都重新扫描**固定目录（不缓存可执行文件路径）。
  - 仅匹配目录下 **`.exe`** 且 **文件名包含关键字** 的文件；选择 **mtime 最新** 的 exe。
  - 直接启动 exe，**不复制/不移动/不写回路径**。
- **图标规则**：卡片图标必须来自该 exe 的**原始图标**（`QIcon(exe_path)` 等系统方式），不得使用自定义图标替代。
- **异常体验**：找不到或启动失败时只弹出友好提示（例如“未找到工具exe文件”），不得抛出未捕获异常导致闪退。

