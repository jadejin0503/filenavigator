```markdown
---
description: PFN 项目约定（PyQt6、路径、线程与自动化）
globs: "**/*.py"
alwaysApply: true
---

# PFN 项目 CursorRules（终版）

## 一、项目核心定位
- **项目全称**：PFN（临床试验项目导航工具）
- **技术栈**：PyQt6 界面 + Windows 剪贴板（CF_HDROP） + SAS EG 自动化 + Z盘网络路径解析
- **核心场景**：左侧项目管理栏（projects/unblinded/users）→ 右侧待办/分析或归类文件树 → 右键操作（复制/粘贴/ SAS EG 打开）
- **开发模式**：Vibe Coding（体感痛点驱动，增量迭代，优先解决「展开慢、路径错、操作卡」核心问题）
- **核心目标**：提升临床试验相关SAS文件操作效率，保证路径解析准确性、UI交互流畅性、功能分发兼容性

## 二、代码核心约定

### 1. 路径处理（精准适配临床试验路径特征）
#### 1.1 路径规范化（强制）
- ✅ 所有路径统一执行：`os.path.normpath(p).replace("/", "\\")`，禁止直接使用原始字符串拼接路径；
- ✅ Z盘判断必须用：`path.upper().startswith("Z:\\")`，兼容大小写输入；
- ❌ 禁止硬编码路径层级（如直接写 `users/project/`），必须通过 `_path_segments_after_root` 动态解析；
- ✅ 路径拼接必须使用 `os.path.join()`，禁止通过 `+` 号拼接路径字符串；
- ✅ 路径长度校验：解析后的Z盘路径长度不得超过Windows最大路径限制（260字符），超长时抛出明确异常。

#### 1.2 Users路径解析（修复核心痛点）
- ✅ Users路径层级强制解析规则：`users/[userid]/projects|project|unblinded/...`，解析时优先调用 `_path_segments_after_root` / `_product_trial_from_path`；
- ✅ 解析逻辑不依赖 `dir_type` 是否含 `-`，需单独校验 `userid` 层级的存在性；
- ✅ 新增路径调试函数：修改解析逻辑后，必须通过 `_debug_path_parse(path)` 验证（输出 `path_type`/`userid`/`层级列表`）；
- ✅ Userid校验规则：userid必须为字母+数字组合（正则：`^[a-zA-Z0-9]+$`），不符合时提示「用户ID格式错误」；
- ✅ 路径解析失败兜底：无法识别的路径类型默认归类为`unknown`，并弹窗提示用户手动选择路径类型。

#### 1.3 剪贴板文件操作（Windows 专属）
- ✅ 强制使用 `ctypes` 实现 CF_HDROP 格式（DROPFILES + UTF-16LE 路径列表），禁止使用 `pyperclip` 等不兼容 CF_HDROP 的库；
- ✅ 剪贴板操作必须包含完整的「打开→读写→关闭」流程：
  ```python
  # 模板：剪贴板操作必加异常捕获+资源释放
  def _clipboard_operate(files):
      try:
          ctypes.windll.user32.OpenClipboard(0)
          # 清空剪贴板原有内容
          ctypes.windll.user32.EmptyClipboard()
          # 核心读写逻辑（CF_HDROP格式）
          ctypes.windll.user32.SetClipboardData(15, ...)  # 15 = CF_HDROP
      except Exception as e:
          raise Exception(f"剪贴板操作失败：{str(e)}")
      finally:
          ctypes.windll.user32.CloseClipboard()  # 强制释放
  ```
- ✅ 剪贴板操作前必须检查是否被占用：调用 `IsClipboardFormatAvailable(15)` 验证CF_HDROP格式可用性；
- ✅ 批量复制文件时，单次剪贴板写入文件数不超过50个，超过时分批处理并提示进度。

### 2. 线程与 UI 交互（解决卡顿/崩溃痛点）
#### 2.1 耗时操作线程化（强制）
- ✅ SAS EG 自动化、Z盘路径遍历、批量文件复制等耗时操作，必须放在 `QThread` 后台线程；
- ❌ 禁止在主线程执行 `time.sleep()`、`pywinauto` 等待等阻塞操作；
- ✅ 线程通信必须使用 `pyqtSignal`，禁止跨线程直接调用对象方法；
- ✅ 线程异常捕获：后台线程必须包含完整的try-except，异常通过信号传递到主线程展示。

#### 2.2 UI 更新规范（修复界面卡死问题）
- ✅ 非主线程更新UI必须使用：
  ```python
  # 推荐模板（简洁且适配所有Qt控件）
  QTimer.singleShot(0, lambda: self.update_status(msg))
  QTimer.singleShot(0, lambda: self.show_error(err))
  ```
- ❌ 禁止直接在后台线程调用 `self.label.setText()` / `self.treeWidget.addItem()` 等UI操作；
- ✅ 状态更新必须包含「操作类型+进度+结果」（如：`✅ 解析users路径：user123/project/HRS7450`）；
- ✅ UI操作防抖：高频更新的控件（如进度条）添加50ms防抖，避免界面闪烁；
- ✅ 长时间操作必须显示进度条：耗时超过1秒的操作（如SAS EG启动），强制展示进度条并实时更新进度。

#### 2.4 SAS 打开分流规则（强制）
- ✅ `.sas` 代码文件：允许 `SAS EG` 自动化打开与 `VS Code` 打开两种入口；
- ✅ SAS 数据集（`.sas7bdat/.sas7bndx/.sas7bcat/.sd2`）：仅允许 `SAS EG` 打开，禁止展示 VS Code 菜单项；
- ✅ 数据集打开必须直接调用 `SEGuide.exe` 并传入文件路径（`subprocess.Popen([seguide_path] + data_paths)`），禁止走服务器树自动化；
- ✅ `SEGuide.exe` 路径必须通过 `_find_sas_eg()` 动态查找，禁止硬编码单一路径；
- ✅ 多选数据集时必须单次启动并一次传入全部路径，避免按文件循环启动多个窗口；
- ✅ `.sas` 自动化逻辑的改动不得影响数据集直开逻辑；数据集直开逻辑的改动不得影响 `.sas` 自动化路径。

#### 2.3 右侧文件树展示长度（列宽）约束（防止回缩）
- ✅ 右侧文件树使用 `QtMainWindow._update_right_tree_columns()` 统一管理列宽；
- ✅ 文件名列（列0）必须保持较宽，至少满足“列0约 70%~72%，列1约 28%~30%”，并设置最小宽度（避免因窗口变窄或刷新被缩短）；
- ✅ 时间列（列1）最小宽度不得低于 `110px`，避免时间显示挤压导致列0被强制回缩；
- ❌ 禁止新增其它逻辑覆盖列宽（如在刷新/排序时重新调用 `setColumnWidth` 且不遵循同一计算规则）。

#### 2.5 左侧「项目管理栏」与右侧视图切换（强制）
- ✅ **左侧标题**：界面展示为「项目管理栏」（非「收藏项目库」）；点击标题区/心形图标可取消左侧树选中，右侧回到「我的待办 / 项目数据分析」标签页。
- ✅ **右侧结构**：使用 `QStackedWidget` 两页切换——页 0 为项目管理（待办 + 数据分析）；页 1 为右侧文件树（含加载提示）。无左侧选中时默认显示页 0；选中左侧可浏览节点时显示页 1。
- ✅ **`current_fav` 与右侧归类（核心）**：左侧节点若能通过 `_favorite_dict_for_selection` 对应到 **`config.json` 中的真实收藏项**（`leaf` / `trial` / `parent` / `pinned_leaf` 解析出的 dict），且 `full_path` 为**有效目录**，则 **必须使用该股收藏 dict 作为 `current_fav`**，调用 `_do_refresh_tree` 走**归类布局**；**禁止**再包一层 `dir_type: "explorer"` 的扁平资源管理器根，否则会丢失下列归类。
- ✅ **projects / unblinded**（`dir_type` 为 `projects` / `unblinded`）：右侧为完整归类树：`data`、`M5`、`program`（`06_programs`/`09_validation` 聚合）、`reports`、各 `documentation` 子路径、`logs`、`util`（`utility/macros|metadata|tools` 聚合）、`Documents` 等，逻辑见 `QtMainWindow._do_refresh_tree` 中 **`not is_users`** 分支。
- ✅ **users**（`dir_type` 以 `users` 开头）：右侧仅保留精简归类：**`program`** 与 **`util`** 两棵聚合（同样基于 `06_programs`/`09_validation` 与 `utility/macros|metadata|tools`），**不**展示 M5、Documents 等整块；逻辑见 `_do_refresh_tree` 中 **`is_users`** 分支。
- ✅ **无收藏 dict 的物理路径**（如产品根节点、`Z:\projects` 等仅有目录、无对应 `favorite_projects` 项）：允许使用 `_to_explorer_fav` + `dir_type: "explorer"`，右侧为懒加载的**扁平目录树**（与 Utility 一致）。
- ✅ **Utility**：点击「Utility 公共目录」时清空左侧选中（建议 `blockSignals` 避免先切回待办页再切目录），右侧页 1 展示 `Z:\projects\utility` 树。
- ✅ **搜索定位**：搜索结果若带收藏 `id` 且能匹配到收藏 dict、且路径为目录，同样**优先使用收藏 dict** 刷新右侧，与点击左侧行为一致。
- ❌ 禁止仅对 `users` 使用收藏 dict、而对 `projects`/`unblinded` 使用 explorer；三类收藏在「有 dict」时必须统一走归类树。

### 3. 配置管理（解决分发/适配痛点）
#### 3.1 配置加载规则（强制）
- ✅ `config.json` 查找优先级（按顺序）：
  1. 程序运行目录 → 2. 上次使用过的 config 所在目录（`%APPDATA%\PFN\pfn_config_dir.txt` 记录）→ 3. `C:\Users\<当前用户>\PFN_Config\config.json`（不存在则创建空配置）
- ✅ 配置加载失败时，自动生成「干净的默认配置」，并提示用户：`"配置文件丢失，已生成默认配置，请重新设置路径"`；
- ✅ 配置项校验：加载配置后必须校验关键项（如SAS EG路径、Z盘根目录），缺失项标记为未配置并引导用户补充；
- ✅ 配置备份：修改配置时自动备份旧配置为 `config.backup.json`，保留最近3个版本。
 - ✅ **分发给新用户**：若仅分发 `PFN.exe`（不附带 `config.json`），对方首次运行会在其机器的 `C:\Users\<对方用户名>\PFN_Config\config.json` 生成并保存收藏；若随 exe 一并分发 `config.json`，则对方会直接使用该配置（等同“带走你的收藏”）。

#### 3.2 打包规范（解决分发问题）
- ✅ **唯一入口**：Windows 发布构建使用仓库根目录 **`build.bat`**（内部调用 `pyinstaller build.spec`），产出 **`PFN_app\PFN.exe`** 单文件；不得再引入独立 onedir spec 或绕过 `main.py` 作为 Analysis 入口。
- ✅ 打包时不将开发者个人 `config.json` 打入 exe；分发用干净配置由程序首次运行生成。
- ✅ 打包后必须验证：新环境运行EXE时，自动生成空白 `config.json`，无硬编码的开发者路径；
- ✅ 打包产物规范：EXE文件必须包含版本信息、开发者信息、版权声明，通过 `pyinstaller` 的 `--version-file` 配置。

#### 3.2.1 Windows 图标与任务栏（强制，保证打包后 logo 一致）
- ✅ **PE / 外壳图标（资源管理器里的 `PFN.exe`）**：以仓库根目录 `icon.ico` 为权威来源；`build.spec` 中 PyInstaller `EXE(..., icon=_exe_icon)` 必须解析到有效 `.ico`（优先根目录 `icon.ico`，否则 `assets/app_icon.ico`）。修改品牌图标时须同步更新上述文件并重新打包验证。
- ✅ **包内资源**：`build.spec` 的 `datas` 须包含 `assets/app_icon.ico`（供冻结环境下 `sys._MEIPASS` 兜底读取），与 `app_qt.py` 主窗口初始化中的兜底路径一致。
- ✅ **单文件分发**：`build.bat` / `build.spec` 仅产出 **单个 `PFN.exe`**；**推荐**在 exe 同目录放置 `icon.ico`（`build.bat` 会复制到 `PFN_app\`），供 `main.py` 的 `_root_icon_path()` 与 `WM_SETICON`；缺失时回退包内 `assets/app_icon.ico`。
- ✅ **任务栏与 Jump List 身份**：`main.py` 在创建 `QApplication` **之前**调用 `ctypes.windll.shell32.SetCurrentProcessExplicitAppUserModelID`（固定 AppID），不得随意改名或后移，否则 Windows 分组与图标缓存行为可能异常。
- ✅ **运行时窗口图标**：主窗口须设置 `QIcon`（`main.py` 中 `app.setWindowIcon` / `window.setWindowIcon`）；Win32 补强使用 `WM_SETICON`（`main.py` 的 `_apply_wm_seticon` 与 `app_qt.py` 的 `_force_taskbar_icon_win32`）时，须与现有逻辑一致。
- ❌ **禁止**在 `SendMessageW(hwnd, WM_SETICON, …, hicon)` 将 `LoadImageW` 得到的 `hicon` 交给窗口后，**立即**对**同一** `hicon` 调用 `DestroyIcon`。窗口仍引用该句柄时销毁会导致**任务栏按钮与任务管理器「窗口」行显示空白默认图标**，而进程行仍显示 exe 内嵌图标（典型误判为「未固定就不显示 logo」）。
- ✅ **每次打包后自检（未固定到任务栏）**：启动分发目录中的 `PFN.exe`，确认任务栏按钮与任务管理器中窗口子项「PFN - 临床试验项目导航」均显示正确 logo。

#### 3.2.2 分发形态（强制：仅单文件）
- ✅ **唯一产物**：`build.bat` + `build.spec` 仅构建 **单文件 `PFN.exe`**（PyInstaller onefile），可复制到任意路径单独运行，**不再维护** onedir（无 `_internal` 目录版）。`build.bat` 将 `assets/DISTRIBUTION_zh.txt` 复制为 `PFN_app\DISTRIBUTION_zh.txt`（批处理文件仅使用 ASCII 路径，避免 cmd 编码解析错误）。

### 4. 命名与结构（增强可维护性）
#### 4.1 命名规则（统一体感）
- ✅ 模块级辅助函数：以 `_` 开头（如 `_strip_prefix`, `_path_segments_after_root`），函数名体现「动作+目标」（如 `_expand_sasapp_tree_main` 而非 `_expand_tree`）；
- ✅ 主窗口类固定为 `QtMainWindow`，核心逻辑集中在 `app_qt.py`，禁止拆分到多个文件；
- ✅ 变量命名：`path_type`（值为 `projects/users_project/users_unblinded`）、`path_levels`（路径层级列表）、`userid`（users下的用户ID），禁止自定义其他命名；
- ✅ 常量命名：全大写+下划线分隔（如 `SAS_EG_WAIT_TIMEOUT = 3`），集中定义在 `constants.py`；
- ✅ 类名规则：采用PascalCase命名法（如 `ConfigManager`），禁止使用小写或下划线；
- ✅ 文件名规则：全小写+下划线（如 `config_manager.py`），禁止使用大写或驼峰命名。

#### 4.2 功能扩展规则（不破坏现有流程）
- ✅ 新增功能必须兼容现有流程：`项目管理栏 → 右侧待办/分析或归类文件树 → 右键操作`，禁止修改核心交互逻辑；
- ✅ 右键菜单目标目录统一计算规则：
  - 选中文件夹 → 目标目录=自身；
  - 选中文件 → 目标目录=文件父目录；
  - 选中多个项 → 目标目录=第一个项的父目录；
  - 新增右键动作时，必须复用此规则，禁止单独计算；
- ✅ 新增功能必须添加开关配置：在 `config.json` 中添加功能开关（如 `"enable_copy_function": true`），支持按需启用；
- ✅ 功能扩展后必须保留回退能力：新增逻辑出现异常时，自动降级到原有逻辑并提示。
- ✅ `program` 聚合（`06_programs` / `09_validation`）目录树内仅展示 `.sas` 文件；`.lst`、`.txt` 等非 SAS 文件默认不展示；
- ✅ `Documents` 节点默认只展示“常用 xlsx”；仅在用户对 `Documents` 节点执行“刷新”后展示该目录下“全部 xlsx”。

#### 4.3 新功能回归保护（强制）
- ✅ 每增加一个新功能，默认行为必须保持“旧功能行为不变”；新逻辑默认关闭或仅在满足特定条件/用户选择时触发；
- ✅ 禁止为了新功能重构替换旧的成功路径；如需修改核心函数，只允许最小兼容补丁，并保留可回退分支；
- ✅ 新功能必须附带“旧功能回归验证点”（至少覆盖：收藏树选中刷新、右键 `打开文件/打开选中文件`、复制文件名、刷新文件夹、`setup.xlsx` 编辑版/参考版打开规则）；
- ✅ 若旧功能出现任何异常或行为改变，必须立即回退到旧逻辑分支并在状态栏提示“已回退到旧逻辑”；
- ✅ 同一提交仅改动一个功能点，禁止一次提交混入多个不相关改动。

## 三、开发与测试规则（补充落地性）
### 1. 迭代优先级（Vibe Coding 核心）
1. 🚨 紧急：解决「SAS EG 展开慢」「users路径解析错误」「文件打不开」等体感痛点；
2. 📌 次优先级：优化「剪贴板复制无弹窗」「UI 无卡顿」「配置自动生成」；
3. ✨ 低优先级：新增功能（如批量操作、快捷键），不影响核心流程时迭代；
4. 🧹 维护优先级：代码重构、注释完善、冗余逻辑清理（迭代间隙执行）。

### 2. 测试必覆盖场景（强制）
| 测试场景 | 验收标准 |
|----------|----------|
| users路径解析 | 输入 `Z:\users\user123\project\HRS7450` → `path_type=users_project`，`userid=user123` |
| users路径异常解析 | 输入 `Z:\users\invalid-id\unblinded` → 提示「用户ID格式错误」，不崩溃 |
| SAS EG 展开 | 从启动到展开目标节点 ≤2秒，users路径不跳转到projects |
| 剪贴板复制 | 右键粘贴到任意文件夹，文件完整且元数据保留，支持批量复制50个文件 |
| 配置分发 | 打包EXE在新电脑运行，自动生成干净config.json，无开发者路径 |
| Windows 图标与任务栏 | 单文件 `PFN.exe` 旁含 `icon.ico`（或由 build.bat 复制到 PFN_app）；未固定任务栏时启动，任务栏与任务管理器窗口行均显示正确 logo（非空白默认窗标） |
| 线程交互 | 批量打开10个文件时，UI无卡顿，进度条实时更新 |
| 路径超长处理 | 输入260+字符Z盘路径 → 提示「路径过长」，不抛出未捕获异常 |
| program 目录展示 | `program` 下仅显示 `.sas`，不显示 `.lst/.txt` |
| Documents 刷新行为 | 默认显示常用 xlsx；刷新后显示全部 xlsx |

### 3. Git 提交规范（适配体感驱动）
- ✅ 提交信息模板：`[类型] 解决的具体痛点 | 影响范围`
  - 示例1：`[fix] 修复users路径解析时忽略userid层级的问题 | 路径解析模块`
  - 示例2：`[opt] 缩短SAS EG节点展开等待时间至2秒 | SAS EG自动化模块`
  - 示例3：`[feat] 新增剪贴板CF_HDROP格式支持，兼容系统右键粘贴 | 文件操作模块`
- ✅ 类型限定：feat（新增）、fix（修复）、opt（优化）、doc（文档）、refactor（重构）、test（测试）、chore（杂项）；
- ✅ 提交粒度：单个提交仅修改一个功能点，禁止一次提交修改多个不相关模块；
- ✅ 提交前必须自检：运行测试用例，确保核心功能无回归问题。

## 四、异常与错误码规范（便于问题定位）
| 错误码 | 错误类型 | 描述 | 解决方案 |
|--------|----------|------|----------|
| E001 | 路径解析 | users路径解析缺失userid层级 | 检查路径格式是否为users/[userid]/xxx，调用_debug_path_parse验证 |
| E002 | SAS EG自动化 | SAS EG节点展开超时 | 增加节点检测超时时间至3秒，检查SAS EG版本是否兼容 |
| E003 | 剪贴板操作 | 剪贴板操作失败（CF_HDROP） | 关闭占用剪贴板的程序，重新执行复制操作 |
| E004 | 配置管理 | 核心配置项缺失 | 引导用户补充配置，使用默认值兜底并提示 |
| E005 | UI交互 | 非主线程更新UI | 改用QTimer.singleShot更新UI，检查线程使用规范 |
| E006 | 路径处理 | Z盘路径长度超限 | 缩短文件名或文件夹名，确保路径长度≤260字符 |
| E007 | SAS EG自动化 | SAS EG进程启动失败 | 检查SAS EG安装路径，验证是否有运行权限 |

## 五、版本号管理规则
- ✅ 版本号格式：主版本.次版本.修订号（如 v1.2.1）
  - 主版本（MAJOR）：核心架构变更（如从 tkinter 迁移到 PyQt6）；
  - 次版本（MINOR）：新增功能/核心痛点修复（如新增剪贴板复制、修复users路径解析）；
  - 修订号（PATCH）：小优化/BUG修复（如缩短等待时间、优化提示文案）；
- ✅ 版本号递增规则：禁止跳过版本号，禁止随意修改主版本号；
- ✅ 发布前必须打Tag：`git tag -a v1.2.1 -m "修复users路径展开到projects的错误"`；
- ✅ 版本日志要求：每个版本必须编写CHANGELOG.md，包含「新增功能」「修复问题」「已知问题」。

## 六、环境依赖规范
### 6.1 必装依赖
| 包名 | 版本 | 安装命令 | 用途 |
|------|------|----------|------|
| PyQt6 | 6.5.1 | pip install PyQt6==6.5.1 | UI界面开发 |
| pywinauto | 0.6.8 | pip install pywinauto==0.6.8 | SAS EG自动化 |
| pywin32 | 306 | pip install pywin32==306 | Windows API调用（剪贴板） |
| python-dotenv | 1.0.0 | pip install python-dotenv==1.0.0 | 环境变量管理 |

### 6.2 系统环境要求
- ✅ 操作系统：Windows 10/11 64位（禁止在32位系统运行）；
- ✅ SAS EG版本：7.1+/9.4+（其他版本需单独适配并标注）；
- ✅ Python版本：3.8~3.10（避免PyQt6兼容性问题）；
- ✅ 权限要求：运行程序需管理员权限（访问Z盘/SAS EG）；
- ✅ 依赖软件：安装VC++ 2019运行库（避免pywinauto运行失败）。

## 七、修改注意事项（强化核心约束）
1. 路径解析函数修改后，必须运行 `test_path_parse.py` 验证所有已知路径（projects/unblinded/users）；
2. SAS EG 自动化逻辑修改后，必须实测「Z盘不同层级文件打开」「users含userid路径展开」；
3. 新增UI交互时，禁止新增弹窗（如复制功能无路径选择弹窗），仅通过状态栏提示；
4. 打包前必须执行 `git clean -xfd` 清理本地配置，确保EXE分发时无开发者个人数据；
5. 修改核心函数（如 `_expand_sasapp_tree_main`）后，必须保留旧版本代码注释，便于回滚；
6. 新增第三方依赖时，必须在 `requirements.txt` 中记录版本号，并验证兼容性；
7. 所有用户可见的提示文案，必须统一格式（如「✅ 操作成功」「❌ 操作失败：原因」），禁止随意编写；
8. 涉及文件操作的功能，必须添加权限校验，无写入权限时提示用户并提供替代方案。

## 八、启动与控制台弹框（强制）
- ✅ Windows 下**禁止**启动时出现“很多一闪而过的 python 控制台/弹框”。
- ✅ 默认启动入口使用 `PFN_silent.pyw`（或命令行 `pythonw PFN_silent.pyw`），避免控制台窗口创建导致闪烁。
- ❌ 禁止把默认启动命令写成 `python app_qt.py`（会创建控制台窗口，即使随后隐藏也可能闪一下）。
- ✅ 保留单实例保护（互斥锁）与控制台隐藏逻辑作为兜底，但**根治方式**仍是 `pythonw/.pyw` 启动。

## 九、setup.xlsx 双开规则（强制，与 README 一致）
- ✅ **禁止**用实例字段缓存「第几次点击」「是否已打开编辑版」等状态；每次打开前必须重新判断。
- ✅ **判定方式**：优先通过 **win32com** 连接已运行的 `Excel.Application`，遍历 `Workbooks`：若其中**任意**工作簿的文件名为 `setup.xlsx`（`FullName` 的基名，**不限路径**），则视为「已有同名工作簿打开」。若 COM 不可用则退回「目标路径独占读是否失败」的弱兜底（**无法覆盖跨目录第二份 setup**，须安装 pywin32）。
- ✅ **第一次（编辑版）**：当 Excel 中**尚无任何**名为 `setup.xlsx` 的工作簿时，仅打开**当前路径原文件**（可编辑）。
- ✅ **第二次及以后（参考版）**：当 Excel 中**已有任意** `setup.xlsx` 工作簿（**包括已打开项目 A 的 setup 再点项目 B 的 setup**），**不得**再对任意网络路径上的 `setup.xlsx` 执行直接打开；应将**当前点击**对应的原文件复制到 **系统 temp 下 PFN 专用目录**（`%TEMP%\\PFN_Reference\\`），文件名为 **`{项目名称}_Setup_参考版.xlsx`**，**覆盖**同名旧文件后，再启动 Excel 打开该临时副本。
- ✅ **关闭 Excel / 重启工具**：行为不变——仍按「当前 Excel 是否已打开原文件」分支，不依赖进程内缓存。
- ✅ 修改此逻辑时须回归：原文件首次可编辑、第二次仅临时参考版、无「同名无法打开」类提示、无残留后台线程误删参考版文件。
```
