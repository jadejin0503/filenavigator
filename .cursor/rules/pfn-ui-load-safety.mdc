---
description: PFN 项目管理页加载安全（禁弹窗、配置容错、主线程 UI）
globs: "**/*.py"
alwaysApply: false
---

# PFN：项目管理页加载安全规则

- **禁止加载期弹窗**：遍历项目/任务/渲染待办等 UI 加载链路中，禁止使用 `QMessageBox`；只允许 `print()` 记录错误并继续（跳过异常数据）。
- **配置解析必须容错**：读取/解析 `config.json` 必须 `try/except`；数据类型不符或字段缺失时：
  - 跳过该项目（不抛异常）
  - 对缺失字段提供默认值（如 `priority="中"`、`status="未完成"`、`tasks=[]`、`milestones={}`）
- **UI 必须主线程更新**：后台线程只做数据整理；所有 UI 更新必须回到主线程（例如信号/`QTimer.singleShot(0, ...)`）。
- **点击去重**：类似“项目管理栏”这种入口应做去重与 in-flight 保护，快速多次点击不得触发重复刷新或并发渲染。

