【PFN 分发说明】（单文件）

1. 分发文件
   运行 build.bat 后，主要产物为：
     PFN_app\PFN.exe
   可将 **仅此一个 exe** 复制到任意目录、桌面或 U 盘后双击使用。

2. 图标（可选）
   同目录放置 icon.ico 时，任务栏/窗口图标与开发环境一致；未放置时使用程序内嵌资源。

3. 配置
   首次运行会在 exe 旁或 %APPDATA%\PFN 等路径生成 config.json；勿将个人 Z 盘路径打包进发给别人的压缩包。
